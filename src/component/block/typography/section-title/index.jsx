import React from 'react'
import styles from './style.scss'

const Typography__Section_Title = (props) => (
  <h2 className={styles.typography__section_title}>{props.text}</h2>
)

export default Typography__Section_Title
